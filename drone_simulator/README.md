# Drone Simulator

Minimal Python digital twin designed to sit behind ASTRA authorization.

## Run

From the repository root:

    python examples/drone_governed_demo.py

The demo moves the simulated vehicle only after ASTRA evaluates the requested
actor, capability, action, resource, scope, and speed limit.
