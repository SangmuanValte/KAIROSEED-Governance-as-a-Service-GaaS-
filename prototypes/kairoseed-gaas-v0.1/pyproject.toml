[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kairoseed-gaas"
version = "0.1.0"
description = "Fail-closed governance gate prototype for agent actions"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
